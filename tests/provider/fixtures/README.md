# Provider TLS fixtures

Integration runners place a short-lived test certificate and private key here as
`certificate.pem` and `private-key.pem`. Credentials and production keys must
never be committed.
