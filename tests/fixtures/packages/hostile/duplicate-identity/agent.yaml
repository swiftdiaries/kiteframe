apiVersion: kiteframe.dev/v1alpha1
kind: Agent
metadata: { name: duplicate-parent, version: 0.1.0 }
spec:
  prompt: { system: prompts/system.md }
  models:
    primary: { capabilities: [text] }
  delegation:
    - agent: agents/first/agent.yaml
    - agent: agents/second/agent.yaml
